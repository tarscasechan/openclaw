---
name: generate-image-for-post
description: Generate a supporting image for a post when a visual genuinely adds value. Use after the written draft and demo pass, not by default.
---

# Generate Image for Post

## Goal

Create an image that supports the article.

Canonical demo policy lives in `../write-post/references/demos.md`. This skill is the image leaf: use it only after prose and simpler demonstrations fail to carry the idea.

## Rules

- Only generate if the image clarifies the core idea.
- Prefer diagrams first for technical posts.
- Keep the prompt tied to the post's conceit.
- If a graph or code block is enough, stop there.

## Output

A single useful image, not decoration.
