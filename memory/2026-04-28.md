# 2026-04-28

- BlueBubbles reminders were misrouting to the raw phone number / `+1` instead of the initiating thread.
- Added `scripts/direct-reminder.sh` to target the exact BlueBubbles chat guid/handle directly for reminders.
- `openai-codex/gpt-5.4-mini` was added to config and set as the default conversational model.
